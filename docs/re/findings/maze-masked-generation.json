{
  "topic": "maze-masked-generation",
  "subagent_run": "2026-06-08 (the LAST background piece: GENERATE the masked-mirror calls the engine emits per frame \u2014 the near-wall vertical FLANK strips \u2014 and wire them into the from-asset call-list generator so a PURE generator reaches the same 99.9% the captured fixture reaches. Builds on maze-masked-mirror.json (masked GEOMETRY byte-exact), maze-generation-law.json (parity branch), maze-index-arithmetic.json + maze-gate-seeding.json + maze-wall-family-seeding.json (the OR families byte-exact).)",
  "binaries": [
    "ega.drv",
    "wmaze.ovr",
    "dosbox_pure_libretro.dylib",
    "mazedata.ega"
  ],
  "status": "CANONICAL CORRIDOR CRACKED + GENERATED BYTE-EXACT. The OR families already reproduced the FULL OR set byte-exact (generateCallist, 78.1% from-asset rendered \u2014 the OR pieces have no near-wall flank). The 78\u219299.9 gap for the canonical maze-corridor (gx127 gy121 facing0) is EXACTLY 4 MASKED-mirror calls drawing the NEAR-WALL VERTICAL FLANK strips (the close corridor walls at the party's immediate left/right sides; placement family imgIdx=1, h=51). Those strips are NEVER drawn by a forward OR-blit \u2014 the engine draws each side's flank as a HORIZONTAL MIRROR of the OPPOSITE side's twin (ega.drv FUN_0a93 masked branch, file 0xbc6; mirror law src.destX + dst.destX + dst.w == 40, byte-exact). A NEW pure generator generateNearFlankMasked(block, party) + generateFullCallList(block, party) (packages/parser/src/maze/callist.ts) emits the canonical 4 flank masked calls {13->4, 10->7, 7->10, 4->13} (all OR-merge) for the gy121-class corridor, and composeBackgroundFromAsset over the GENERATED full call list reaches 99.909% (19694/19712) of the maze-corridor engine viewport \u2014 IDENTICAL to the captured-fixture diagnostic, now fully generated (no captured frame). The masked SET was confirmed reproducible byte-exact across 3 fresh pokeview captures of gy121. RESIDUE (documented, anti-overfit): for OTHER open-passage views the flank SUBSET that fires + the deeper door-recess masked pairs OSCILLATE run-to-run (the same mid-build non-determinism the parity-odd pairing shows). Only the gy121-class corridor (deep, straight, both-open, parity-even facing-0) is byte-exact stable; the rest is the ray-march residue.",
  "reference_frame": {
    "patched_core": "tools/libretro/dosbox_pure_libretro.dylib (nm -gU | grep _dbp_ = 11 exports; build-core.sh).",
    "or_blit": "ega.drv FUN_0a93 (file 0xa93). reloc base stable 0x6ba10 across fresh boots this pass (WIZ6_ORBASE override).",
    "cell_data": "extracted/maze level-0 (tools/parity/fixtures/engine/maze-frames.json mazeBlock). Region 0: cellA=gy-116, cellB=gx-120. Party fields facing 0x4f9a / z 0x4f9c / cellA 0x4f9e / cellB 0x4fa0 / gy 0x4fa2 / gx 0x4fa4."
  },
  "flank_family": {
    "claim": "The near-wall vertical FLANK family is the mazedata.ega placements with imgIdx=1 (image w=8 h=51 \u2014 the close corridor side walls). There are exactly 5, organized as a CENTER self-mirror + two mirror PAIRS keyed by `count`:",
    "records": {
      "1": {
        "destX": 16,
        "bias": 0,
        "count": 8,
        "role": "CENTER (self-mirror about page col 20)"
      },
      "4": {
        "destX": 8,
        "bias": 1,
        "count": 4,
        "role": "LEFT  (count-4 pair)"
      },
      "13": {
        "destX": 24,
        "bias": 3,
        "count": 4,
        "role": "RIGHT (count-4 pair)"
      },
      "7": {
        "destX": 8,
        "bias": 5,
        "count": 3,
        "role": "LEFT  (count-3 pair)"
      },
      "10": {
        "destX": 24,
        "bias": 0,
        "count": 3,
        "role": "RIGHT (count-3 pair)"
      }
    },
    "mirror_pairs": "count-4: LEFT 4 (destX 8) <-> RIGHT 13 (destX 24); count-3: LEFT 7 (destX 8) <-> RIGHT 10 (destX 24). For each pair, leftX + rightX + w(8) = 8 + 24 + 8 = 40 = the page-row byte count (mirror about col 20 / screen px 160)."
  },
  "findings": [
    {
      "id": "near-flank-drawn-only-by-masked-mirror",
      "claim": "The near-wall flank strips (imgIdx=1: {1,4,7,10,13}) are NEVER drawn by a forward OR-blit \u2014 they appear in ZERO of the captured OR sets across all 11+ captured views. The engine draws them ONLY through the masked-mirror branch (ega.drv FUN_0a93 file 0xbc6): each side's flank is the HORIZONTAL MIRROR of the opposite-side twin. For the canonical corridor (gy121) all 4 flank calls fire \u2014 dst 4 <- src 13, dst 7 <- src 10, dst 10 <- src 7, dst 13 <- src 4 \u2014 all OR-merge (masked flag = 1). This is exactly the 78.1%->99.909% from-asset gap (the OR families compose the ceiling/floor/side walls but leave the near flanks black).",
      "category": "render_algorithm",
      "evidence": {
        "binary": "ega.drv",
        "address": {
          "space": "ega.drv",
          "offset": "0xbc6"
        },
        "type": "capture_decomposition_plus_from_asset_compose",
        "details": "Decomposed the OR vs masked branches of all captured views (docs/re/findings/maze-views/*.json): the imgIdx=1 indices {1,4,7,10,13} never appear as an OR arg0c, only as masked arg0c/arg10. For gy121 the masked calls are {13->4, 10->7, 7->10, 4->13} (flag=1). composeBackgroundFromAsset over generateCallist's OR set ALONE = 78.1%; over generateFullCallList (OR + the 4 flank masked) = 99.909% (19694/19712 vs the maze-corridor.idx.gz viewport). The mirror law src.destX + dst.destX + dst.w == 40 holds byte-exact for every captured masked pair where both records are present (verified inline in the pokeview harness \u2014 'mirror-law-OK')."
      },
      "confidence": "high"
    },
    {
      "id": "near-flank-firing-gate-open-passage",
      "claim": "The near-flank masked family fires only when the party stands in an OPEN PASSAGE: the LEFT and RIGHT corner edges at the party's OWN (depth-0) cell are both OPEN (code 0) AND the forward edge is open (code 0), AND the frame is parity-EVEN facing-0 (the forward-OR branch). When a flank corner is STONE the near wall is drawn by the OR side-wall family instead and NO flank masked fires \u2014 confirmed byte-exact: v6 (closed front d0), v7 (cR stone), v9 (cL stone), v10 (cR stone, facing 1) capture ZERO near-flank masked calls, and generateNearFlankMasked returns [] for all four. Parity-ODD frames draw the WHOLE frame through the masked branch (a different, run-to-run-oscillating case \u2014 maze-generation-law.json), so the deterministic flank-masked emission is parity-EVEN only.",
      "category": "render_algorithm",
      "evidence": {
        "binary": "wmaze.ovr",
        "address": {
          "space": "wmaze.ovr",
          "offset": "0x4c45"
        },
        "type": "capture_cross_reference",
        "details": "Per-view depth-0 corner profile (maze-geometry.ts cornerL/cornerR/forwardEdge on maze-frames.json): v1 gy121f0 cL0=0 cR0=0 F0=0 -> 4 masked flanks; v5 gx125f0 cL0=0 cR0=0 -> flanks (subset, oscillates); v11 gx123gy122f1 cL0=0 cR0=0 -> flanks; v6 gy123f0 cL0=2 cR0=2 (closed front) -> 0; v7 gx121gy119f0 cR=2 stone -> 0; v9 gx123gy121f0 cL=2 stone -> 0; v10 gx124gy121f1 cR=2 stone -> 0. The gate (both d0 corners open + forward open + parity-even) reproduces the FIRE/NO-FIRE split byte-exact across all 7 deterministic views."
      },
      "confidence": "high"
    },
    {
      "id": "canonical-corridor-byte-exact-reproducible",
      "claim": "The canonical maze-corridor (gx127 gy121 facing0) masked set {13->4, 10->7, 7->10, 4->13} (all OR-merge) is REPRODUCIBLE byte-exact across 3 fresh pokeview captures (no run-to-run variation). The pure generator generateFullCallList reproduces it and composes (from mazedata.ega alone) to 99.909% of the engine viewport \u2014 the SAME 19694/19712 the captured-fixture diagnostic reaches, now generated. The 18px residual is the deep-door-center detail (a draw path beyond the OR/masked background blit, NOT a missing placement \u2014 exhaustively verified in the prior diagnostic).",
      "category": "render_algorithm",
      "evidence": {
        "binary": "dosbox_pure_libretro.dylib",
        "type": "live_capture_reproducibility_plus_pure_generator",
        "details": "pokeview gx127 gy121 f0 captured 3x (WIZ6_ORBASE=0x6ba10): each = 31 calls (27 OR + 4 masked), masked = [13->4,10->7,7->10,4->13], flags [1,1,1,1] \u2014 byte-identical. Pure generator: packages/parser/src/maze/callist.ts generateNearFlankMasked + generateFullCallList; gated by packages/parser/tests/maze/gen-callist-parity.test.ts ('byte-exact for the canonical maze-corridor' + 'the GENERATED full call list composes to >=99.9% of the gy121 oracle viewport' = 19694/19712)."
      },
      "confidence": "high"
    },
    {
      "id": "other-open-passage-flank-subset-oscillates",
      "claim": "RESIDUE (anti-overfit): for OTHER open-passage parity-EVEN views the flank SUBSET that fires + any deeper door-recess masked pairs OSCILLATE run-to-run (the same mid-build non-determinism the parity-odd pairing shows \u2014 maze-generation-law.json parity-odd-pairing-oscillates). Captured: v1 gy121 draws all 4 flanks (STABLE); v5 gx125 draws {7->10,4->13,1->1}; v11 gx123gy122f1 draws {13->4,10->7}; gx121gy118f1 draws {10->7,7->10} (STABLE 2x); gx124gy122f0 drew {13->4,10->7} on one run and {13->4,10->7,33->30,30->33,24->24,27->27} on the next (OSCILLATES). So generateNearFlankMasked emits the SYMMETRIC 4-flank set for every open passage (byte-exact for gy121, valid mirror calls for the others) and the deeper door-recess specials are left as residue. The over-emitted flanks for the oscillating views are all VALID near-flank mirror calls (mirror-law-correct, OR-merge); they are not byte-exact targets because the engine's per-frame subset is not deterministic.",
      "category": "render_algorithm",
      "evidence": {
        "binary": "ega.drv",
        "type": "multi_run_reproducibility",
        "details": "gy121 captured 3x identical (STABLE). gx121gy118f1 captured 2x identical {10->7,7->10} (STABLE). gx124gy122f0 captured 2x DIFFERENT masked sets (OSCILLATES). v5/v11 single captures show flank subsets that differ from gy121's full set. The pokeview harness flags `reproducible` per the first-OR-recurrence pass extraction; the masked SET stability is a separate per-cell property (deterministic only for the gy121-class deep straight corridor)."
      },
      "confidence": "high"
    }
  ],
  "generation_law": {
    "one_liner": "near-flank masked fires iff the party is in an OPEN PASSAGE (both depth-0 corners open + forward open) AND the frame is parity-EVEN facing-0. The fired calls draw both screen-side flanks of each mirror PAIR (count-4: 4<->13, count-3: 7<->10), each as the masked OR-merge mirror of the opposite-side twin: src=opposite, dst=this side. byte-exact for the canonical gy121-class corridor.",
    "generator": "packages/parser/src/maze/callist.ts: generateNearFlankMasked(block, party) returns the BackgroundCall[] of masked flank calls; generateFullCallList(block, party) = generateCallist OR set (as kind:'OR') + generateNearFlankMasked. composeBackgroundFromAsset over generateFullCallList = 99.909% for maze-corridor.",
    "mirror_law": "src.destX + dst.destX + dst.w == 40 (mirror about page col 20). Inherited byte-exact from maze-masked-mirror.json; re-verified inline in the pokeview harness for every captured masked call.",
    "residue": "the per-view flank SUBSET + the deeper door-recess masked pairs oscillate run-to-run for non-canonical open-passage views (the perspective ray-march residue, the same decompiler-resistant classifier post-pass that gates the OR side-wall extent). The parity-ODD whole-frame masked branch is a separate oscillating case."
  },
  "captured_views": [
    {
      "name": "v1-gy121f0 (committed oracle)",
      "gx": 127,
      "gy": 121,
      "facing": 0,
      "parity": 0,
      "masked": "[13->4,10->7,7->10,4->13]",
      "stable": true,
      "note": "the canonical maze-corridor; 3 fresh captures byte-identical; generated byte-exact + 99.909% from-asset."
    },
    {
      "name": "v12-gx121gy118f1 (NEW this pass)",
      "gx": 121,
      "gy": 118,
      "facing": 1,
      "parity": 0,
      "masked": "[10->7,7->10]",
      "stable": true,
      "note": "deep open-passage parity-even view; 2 fresh captures byte-identical; committed with masked DST geometry in liveRecords (mirror-law verified)."
    },
    {
      "name": "v5-gx125f0",
      "gx": 125,
      "gy": 121,
      "facing": 0,
      "parity": 0,
      "masked": "[7->10,4->13,1->1]",
      "stable": false,
      "note": "open passage; flank subset differs from gy121 (residue)."
    },
    {
      "name": "v11-gx123gy122f1",
      "gx": 123,
      "gy": 122,
      "facing": 1,
      "parity": 0,
      "masked": "[13->4,10->7]",
      "stable": false,
      "note": "open passage; flank subset = LEFT pair only (residue)."
    },
    {
      "name": "gx124gy122f0 (not committed)",
      "gx": 124,
      "gy": 122,
      "facing": 0,
      "parity": 0,
      "masked": "OSCILLATES",
      "stable": false,
      "note": "two captures gave different masked sets \u2014 the mid-build non-determinism."
    }
  ],
  "asm_anchors": [
    {
      "space": "ega.drv",
      "offset": "0xbc6",
      "name": "ega_masked_mirror_branch",
      "note": "the masked-mirror blit branch (arg [bp+0x10] != 0xffff). src image = placement arg0c; dst geometry = placement arg10; OR-merge (flag != 0) per the near-flank family. Geometry byte-exact in maze-masked-mirror.json."
    },
    {
      "space": "wmaze.ovr",
      "offset": "0x4c45",
      "name": "frame_parity",
      "note": "parity=(gx+gy+facing)%2 \u2014 parity-EVEN selects the forward-OR branch (the near flanks then come through the masked branch as the few mirror calls); parity-ODD draws the whole frame through the masked branch (oscillating)."
    }
  ],
  "artifacts": [
    "packages/parser/src/maze/callist.ts \u2014 NEW generateNearFlankMasked(block,party) + generateFullCallList(block,party) + FLANK_MIRROR_PAIRS; the near-flank masked generation law.",
    "packages/parser/src/maze/index.ts \u2014 exports generateNearFlankMasked + generateFullCallList.",
    "packages/parser/tests/maze/gen-callist-parity.test.ts \u2014 NEW gate: (a) masked set byte-exact for gy121, (b) no-masked for stone-corner/closed-front views, (c) mirror law for every generated masked call, (d) from-asset viewport >=99.9% for maze-corridor (19694/19712) via the GENERATED full call list.",
    "tools/libretro/trace-maze.ts \u2014 pokeview now records the masked DST geometry (arg10) in liveRecords + verifies the mirror law inline ('mirror-law-OK'). (The masked-mirror BRANCH was already captured by phasePlacements at base+0xbca/cmp+masked_flags \u2014 this pass added the dst-geometry record + the inline law check.)",
    "docs/re/findings/maze-views/v12-gx121gy118f1.json \u2014 NEW stable parity-even open-passage masked capture (with masked dst geometry)."
  ],
  "remaining": [
    "OTHER OPEN-PASSAGE FLANK SUBSET + DEEP DOOR-RECESS MASKED PAIRS: the per-view flank subset that fires (and the deeper door-recess masked pairs, e.g. v1's far-door masks) oscillate run-to-run for non-canonical views \u2014 the perspective ray-march residue. A navigation-reach depth-keyed BUILD trace (not a poke-recompose, which doesn't re-run the build loop \u2014 maze-wall-family-seeding.json blocked-live-trace) is the unblock.",
    "PARITY-ODD WHOLE-FRAME MASKED BRANCH: a separate oscillating case (maze-generation-law.json). Not addressed this pass.",
    "DEEP-DOOR 18px RESIDUAL: the from-asset viewport floor is 99.909%; the residual 18px deep-door-center detail is a draw path beyond the OR/masked background blit (not a missing placement) \u2014 tracked in TODO, unchanged."
  ],
  "concerns": [
    "The near-flank masked SET is byte-exact + reproducible ONLY for the gy121-class corridor (deep, straight, both-open, parity-even facing-0). For other open-passage views generateNearFlankMasked over-emits the symmetric 4-flank set (all valid mirror calls) where the engine's per-frame subset is non-deterministic \u2014 this is documented residue, NOT a byte-exact claim. Do NOT tune the gate to match a single oscillating capture (it would overfit to a transient).",
    "The mirror law + masked GEOMETRY are byte-exact (maze-masked-mirror.json, independently gated). This pass adds only the per-view SELECTION of WHICH flank pairs fire \u2014 high confidence for the canonical corridor, residue for the rest.",
    "All runtime addresses heap-dependent; WIZ6_ORBASE=0x6ba10 was stable this pass \u2014 re-derive per run via the resolver if the heap shifts."
  ],
  "caveat_for_prior_findings": "EXTENDS maze-masked-mirror.json (the masked GEOMETRY was byte-exact; this pass adds the per-view SELECTION of the near-wall flank masked calls and a PURE generator that emits them) and maze-wall-family-seeding.json (the OR families were byte-exact; the near flanks are the masked complement that was the 78->99.9 gap). CONFIRMS maze-generation-law.json (parity selects OR-vs-masked; the parity-EVEN frame's masked calls are the SPECIFIC near-flank pieces, not a whole-frame mirror \u2014 reconciling the parity-EVEN-has-masked observation: the near flanks ALWAYS come through the masked branch; the parity law governs the whole-frame case for parity-ODD). The from-asset whole-VIEWPORT gate is now 99.909% from a PURE generator (no captured frame) for the canonical corridor \u2014 closing the masked-generation half of the from-asset gate (the deep-door 18px residual remains, tracked separately).",
  "off_axis_generalization_2026_06_09": {
    "status": "OFF-AXIS VIEWS GENERALIZED (NOT byte-exact \u2014 dither + door-recess residue). Fixed the user's 'walking around shows garbage / wrong angles / black voids' complaint for the 6 captured off-axis ground-truth views by generalizing two pieces of the OR side-wall generator + tightening the near-flank gate. The DECISIVE finding: byte-exact generation of these views is NOT attainable AND the captures themselves do not reproduce their fixtures via the from-asset compose \u2014 composing the engine's OWN captured call-list through composeCallList reaches only 29-84% (the off-axis masked-mirror pairings don't satisfy the col-20 law via static maskedMirrorFor, and the dithered receding-stone surfaces land a different dither phase than the engine's runtime surface). So the goal shifted (per the task's fallback clause) to: render the views as recognizable CORRIDORS not black voids, strictly improve every view through the wired path, and emit NO spurious wrong-angle walls.",
    "two_generalizations": [
      "(1) SYMMETRIC SIDE-WALL FILL. Removed the `isAsymmetricResidueSide` SUPPRESSION in generateSideWall that zeroed the receding side wall of turned corridors (the black void). The cumulative trapezoid stack (LEFT bases {134,130,126}+p, RIGHT {138,142,146}+p, +28 floor twins) now emits on BOTH sides, gated only by each side's OWN corner edge. The entrance (gy121 f0) is unchanged (both sides open -> identical set), and the emitted indices stay a SUBSET of every gated capture (v1/v5/v6/v7/v10/v11). Wired-path lift: gx124gy121f3 +9pp, gx126gy121f3 +14pp.",
      "(2) FULL-HEIGHT STONE-SIDE WALL (generalized). A STONE side corner draws a receding full-height wall: base 15 (LEFT) / 19 (RIGHT) at base+d. The PRIOR gate (`facing===0 && side==='right' && leftAllOpen`) only drew the v7 RIGHT-stone case -> every other turned corridor's stone side was BLACK. Generalized to any (side,facing), EXCLUDING (a) the near depth of a stone run that starts at the party's own cell (the near occluding piece is a different/larger placement -- v10 has 20,21,22 not 19; gx126gy121f3 has 16,17 not 15), and (b) the occlusion-stop depth when its front is a DOOR (code 3) -- v1's stop is a door, capped by the door-recess family, no 15/19 there; a SOLID-front stop still draws the wall. This rule emits ZERO spurious indices across v1/v6/v7/v10/v11 + all 6 freeroam captures. Wired-path lift: gx126gy121f3 +21pp (77->84), gx127gy122f3 +11pp (72->84).",
      "(3) NEAR-FLANK STRAIGHT-CORRIDOR GATE. generateNearFlankMasked now also requires the passage to stay open at DEPTH 1 (both depth-1 corners open) -- the symmetric flank mirror is only correct for a straight corridor. When a side corner goes stone at depth 1 (gx127gy122f3, cornerR=2) the canonical 4-flank set is SPURIOUS and measurably REGRESSES the view (82%->72%); the gate now suppresses it there. The entrance / v5 / v11 corridors keep depth-1 corners open and still fire (the gen-callist mirror-law gate stays green)."
    ],
    "per_view_wired_parity_floor": {
      "note": "Match count / 19712 through generateFullCallList -> composeCallList -> renderMazeViewport vs the committed maze-freeroam-<view>.idx.gz. BEFORE = current generator before this pass; AFTER = generalized. Gated by packages/parser/tests/maze/maze-freeroam-parity.test.ts (per-view regression floor + no-spurious-garbage).",
      "gx124gy121f0": {
        "before_pct": 48.67,
        "after_pct": 48.67,
        "after_match": 9594,
        "residue": "in-place-turn capture (mid-build garbage masked_flags); door-recess + dither"
      },
      "gx124gy121f3": {
        "before_pct": 47.43,
        "after_pct": 56.82,
        "after_match": 11200,
        "residue": "door-recess family + dither"
      },
      "gx126gy121f3": {
        "before_pct": 63.62,
        "after_pct": 84.18,
        "after_match": 16594,
        "residue": "dither (LEFT stone wall now filled, was black void)"
      },
      "gx127gy121f1": {
        "before_pct": 48.36,
        "after_pct": 48.36,
        "after_match": 9532,
        "residue": "front-occlusion stop cuts view at depth1 while open side recedes (off-axis classify) + dither"
      },
      "gx127gy122f3": {
        "before_pct": 72.4,
        "after_pct": 83.72,
        "after_match": 16503,
        "residue": "dither (RIGHT stone wall now filled; spurious near-flank suppressed)"
      },
      "gx127gy123f1": {
        "before_pct": 37.44,
        "after_pct": 37.44,
        "after_match": 7381,
        "residue": "front-door junction; door-recess family is decompiler-resistant"
      }
    },
    "residue_classes": [
      "DITHER-PHASE: the receding stone-wall / cobble-floor surfaces are dithered 2-index patterns; static-placement compose lands a different dither phase than the engine's runtime surface (same class as the entrance's 18px deep-door gap, larger surface area). Caps the 'looks-correct' views at ~84-89%. NOT fixable by adding placements.",
      "DOOR-RECESS FAMILY (138-149 + 166-177): per-depth selection lives in the decompiler-resistant classify post-pass (maze-wall-family-seeding.json 0x39ec). Dominates gx127gy123f1 (front-door junction) and parts of gx124gy121f3/gx126gy121f3.",
      "OFF-AXIS FRONT-OCCLUSION: computeVisibleDepths (front==2 stop) is byte-exact for facing-0 (v1/v2/v5/v6) but cuts gx127gy121f1's view at depth1 while the open LEFT side recedes further. Extending the side-wall past the stop was rejected -- it adds spurious indices to v5 (137,165,141,169 not in capture), breaking the generated-subset-captured gate. So the stop stays and gx127gy121f1 holds at the OR floor."
    ],
    "artifacts": [
      "packages/parser/src/maze/callist.ts -- generateSideWall (removed isAsymmetricResidueSide suppression; generalized full-height stone-wall to STONE_WALL_BASE{left:15,right:19} with near-run-start + door-stop exclusions); generateNearFlankMasked (added depth-1 straight-corridor gate).",
      "packages/parser/tests/maze/maze-freeroam-parity.test.ts -- NEW gate: per-view wired-path pixel floor + no-spurious-garbage (generated placed-index subset of engine PLACED set modulo enumerated residue) for the 6 off-axis views.",
      "docs/re/findings/maze-views/freeroam-*.json (the 6 captured ground-truth call-lists, used as the no-spurious oracle) + tools/parity/fixtures/engine/maze-freeroam-*.idx.gz (the pixel oracle)."
    ],
    "confidence": "high for the generalizations being net-positive + spurious-free (gated); honest about the residue (byte-exact NOT achieved; the captures don't even reproduce themselves via compose). The dither + door-recess + off-axis-front-occlusion residues are the genuine remaining RE work, tracked here, not papered over."
  },
  "edge_junction_void_fix_2026_06_09": {
    "status": "EDGE/JUNCTION BLACK-VOID FIXED. Follow-up to off_axis_generalization_2026_06_09: the residual void views (gx127gy121f1 truncated at depth 1; gx124gy121f0 centre void band; gx127gy123f1) are no longer void.",
    "root_cause": "The generator (a) truncated turned corridors at the first front=2 even when the corridor jogs past it (an offset wall with one stone side), (b) skipped the receding stone side wall reached through a near door, and (c) drew no far wall at a deep solid-front stop. All three left black where the engine draws walls.",
    "fix": "Three OR-direct laws in callist.ts (see maze-wall-family-seeding.json off_axis_void_fix_2026_06_09): offset-wall occlusion exception (front=2 + one stone side = jog -> see to depth 3), receding-stone-wall-through-passage (emit base 15/19+d when a passage precedes the stone), deep-solid far-wall (emit {0,83,87}+stop for a solid stop at depth>=2).",
    "fixture_validation": "Built tools/libretro/validate-freeroam-fixture.ts: composes each fixture's OWN engine call-list through the wired viewport path and measures self-reproduction. The 6 framebuffers ARE settled (byte-identical on re-capture with +60 settle frames); only the CALL-LISTS were mid-build (in-place-turn captures, masked-heavy). Re-captured f0 (97 OR+328 masked full build) + f1 (37 OR full recompose). The masked-mirror side-wall family (gx124gy121f0 sides) + the door-recess archway (gx127gy123f1) remain the genuine decompiler-resistant residue \u2014 NOT generated OR-direct.",
    "confidence": "high (eyeball-confirmed corridors; all gated tests green)"
  },
  "lookback_headon_door_fix_2026_06_09": {
    "status": "LOOK-BACK (head-on door) FIXED. The user-reported 'turning around to look back at the entrance gate renders wrong — a closed green portcullis with a BLACK GAP between it and the right wall'. Drove the engine to free-roam facing 2 at gx127 gy121 + gy122 (the look-back), captured the SETTLED framebuffer (byte-identical on re-capture) + call-list.",
    "root_cause": "The generator did NOT treat a DOOR (north=3) viewed HEAD-ON (facing 2/3) as an occluder — computeVisibleDepths only occluded front==3 when BOTH side corners were stone. The look-back door has OPEN side corners, so the generator walked to depth 3: a deep receding corridor with the cornerR=2 stone wall drawn partial = the 'black gap', and the gate floating small/deep instead of the engine's near, large head-on gate.",
    "fix": "frontOccludes(front,cL,cR,headon): a head-on door (front==3 && facing in {2,3}) occludes at its depth (classify.ts law (4) head-on read direction). gy121-f2 caps at depth 0 (closed-front gate family {0,83,87} fills the viewport); gy122-f2 caps at depth 1 (far-closed gate {1,84,88} at the corridor end). See maze-wall-family-seeding.json lookback_headon_door_fix_2026_06_09 for the full law + per-view results.",
    "fixture_validation": "Engine framebuffers byte-identical on re-capture = SETTLED ground truth (eyeball-confirmed near + far gate, no void). The call-list self-repro is LOW (39-44%) — the SAME masked-mirror static-compose limitation as the other off-axis door views (the engine draws the gate via the door-recess masked family, which static maskedMirrorFor can't reproduce). Structural gate + eyeball is the success measure; the door-leaf detail (17-34 / 5-34) is decompiler-resistant residue.",
    "confidence": "high (eyeball-confirmed gate, no void/gap; no-regression on all prior captures — no prior view has a head-on door)"
  }
}